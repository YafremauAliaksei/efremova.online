import { notFound } from 'next/navigation';

/**
 * Любой неизвестный адрес под языком (/pl/nie-ma) попадает сюда и отдаёт
 * 404 внутри языкового макета: посетитель видит страницу «не найдено» на
 * своём языке, с переключателем и подвалом, а не голый ответ сервера.
 */
export default function UnknownPage() {
  notFound();
}
