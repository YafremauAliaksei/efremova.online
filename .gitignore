# ─── Секреты. Попадание сюда в git = инцидент безопасности ───
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
.secrets/
*.age
terraform.tfvars
*.tfstate
*.tfstate.*
.terraform/

# ─── Зависимости и сборка ───
node_modules/
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ─── Данные и бэкапы (никогда не в репозитории) ───
*.sql
*.dump
# ⚠️ Исключение из правила выше. Миграции — это НЕ данные, а описание
# структуры базы, и они обязаны быть в репозитории: без них на сервере
# создастся пустая база. Правило «*.sql» едва не увело их из git молча.
!prisma/migrations/**/*.sql
backups/
postgres-data/
redis-data/
uploads/

# ─── Отчёты инструментов ───
coverage/
.lighthouseci/
zap-report*
trivy-report*
gitleaks-report*

# ─── Личные настройки Claude Code ───
# .claude/settings.json — общий для проекта, коммитится (правила безопасности).
# settings.local.json — личные разрешения конкретного разработчика: не коммитится.
.claude/settings.local.json

# ─── Системное ───
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea/
