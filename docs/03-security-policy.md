# 🛡️ 03 — Политика безопасности и ловушки для сканеров

Относится к основному домену — сайту-визитке. Личных данных клиентов на нём
нет (docs/13, раздел 10); требования к кабинету на поддомене будут описаны
вместе с ним.

---

## 1. Рубежи обороны

Пробить один слой должно быть недостаточно.

```
   Атакующий
       │
   ┌───▼──────────────────────────────────────────────┐
 1 │ CLOUDFLARE: Anti-DDoS, WAF, Bot Management       │  гасится ДО сервера
   ├──────────────────────────────────────────────────┤
 2 │ NGINX: TLS 1.3, rate limit, лимит размера тела   │  гасится ДО приложения
   ├──────────────────────────────────────────────────┤
 3 │ MIDDLEWARE: CSP с nonce, CSRF, honeypot          │  до логики приложения
   ├──────────────────────────────────────────────────┤
 4 │ ПРИЛОЖЕНИЕ: вход в админку, Zod-валидация        │  проверка прав на каждое действие
   ├──────────────────────────────────────────────────┤
 5 │ БАЗА: только параметризованные запросы (Prisma)  │
   ├──────────────────────────────────────────────────┤
 6 │ ЖУРНАЛ: security_events, honeypot_hits           │  видно, кто и как лез
   └──────────────────────────────────────────────────┘
```

---

## 2. Шифрование

| Где | Как |
|---|---|
| В пути | только TLS 1.3; HSTS `max-age=63072000; includeSubDomains; preload` |
| На диске | шифрованный том сервера; ночная резервная копия базы шифруется (`age`) — делается вместе с развёртыванием |
| Секреты | только в `.env` на сервере; в git, коде и логах — никогда |

---

## 3. Защита от конкретных атак

| Атака | Как закрыто |
|---|---|
| SQL-инъекция | Prisma параметризует всё; сырой SQL запрещён линтером |
| XSS | React экранирует; `dangerouslySetInnerHTML` запрещён правилом ESLint; CSP с nonce |
| CSRF | `SameSite=Lax` + проверка `Origin` / `Sec-Fetch-Site` на изменяющих запросах |
| SSRF | исходящие запросы сервера — только на заранее известные адреса |
| Кража сессии админа | cookie `__Host-`, httpOnly, Secure, 30 минут жизни |
| Clickjacking | `frame-ancestors 'none'` + `X-Frame-Options: DENY` |
| Прямой доступ к серверу мимо Cloudflare | 80/443 принимаются только с IP-диапазонов Cloudflare |
| Уязвимые зависимости | `npm audit` + Trivy + Dependabot в CI, lock-файл зафиксирован |

---

## 4. 🍯 Honeypot: ловушки для сканеров

### 4.1 Идея

Обычный посетитель никогда не откроет `/wp-admin/`, не заполнит невидимое поле
и не полезет в `/.env`. Кто это сделал — сканер уязвимостей. Ложных
срабатываний у ловушки нет: попасть в неё случайно нельзя.

### 4.2 Пять типов ловушек

```
ТИП 1 — ФАЛЬШИВЫЕ ПУТИ
  /wp-admin/, /wp-login.php, /.env, /.git/config, /phpmyadmin/,
  /admin.php, /config.json, /backup.sql, /.aws/credentials, /api/v1/debug
  → 404; первое попадание с адреса — с задержкой 2–5 с

ТИП 2 — НЕВИДИМОЕ ПОЛЕ ФОРМЫ
  <input name="website_url" tabindex="-1" aria-hidden="true">,
  спрятано CSS-ом, не display:none → заполнено = бот; форма «принята» и никуда не уходит

ТИП 3 — ПРИМАНКА В robots.txt
  Disallow: /internal-admin-v2/ → честный краулер послушается, сканер полезет туда

ТИП 4 — ФАЛЬШИВЫЙ API
  /api/internal/users?export=all → правдоподобные данные с email-«канарейками»:
  если такой адрес всплывёт, известны дата и IP источника

ТИП 5 — ВРЕМЕННАЯ ЛОВУШКА
  форма отправлена быстрее 1,5 с после загрузки → человек так не успевает
```

### 4.3 Что происходит после срабатывания

Запись идёт через ограничитель (`src/lib/security/throttle.ts`) и склеивается
в строку «адрес + тип + час» со счётчиком — подробно в docs/13, раздел 9.
Автоблокировка в Cloudflare отложена: поле `pushedToCloudflare` заполнится,
когда появится отдельная задача.

### 4.4 Защита от ложных срабатываний — понадобится вместе с автоблокировкой

Пока ловушки только записывают, ложное срабатывание ничего не стоит.
Когда появится автоблокировка, до неё обязательны:

| Риск | Страховка |
|---|---|
| Поисковый бот полезет в приманку | проверка reverse-DNS настоящих ботов Google/Bing |
| Мониторинг сам себя поймает | IP мониторинга в постоянном allowlist |
| Пентестер по договору | заголовок `X-Pentest-Token` отключает ловушки |

---

## 5. Заголовки безопасности (реализованы в коде)

```
Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
Content-Security-Policy: default-src 'self'; script-src 'self' 'nonce-{RANDOM}' 'strict-dynamic';
                         frame-ancestors 'none'; base-uri 'none'; form-action 'self';
                         object-src 'none'; upgrade-insecure-requests
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), interest-cohort=()
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Embedder-Policy: require-corp
Cross-Origin-Resource-Policy: same-origin
```

Nonce генерируется заново на каждый запрос: вставленный в страницу `<script>`
без него браузер не выполнит. Поэтому запрещены inline-скрипты без nonce,
`eval()` и любые виджеты, требующие `unsafe-inline`. Сборка: `src/lib/security/headers.ts`.

---

## 6. Конвейер безопасности в CI

| Этап | Инструмент | Блокирует слияние |
|---|---|---|
| Секреты | Gitleaks (вся история) | да |
| SAST | Semgrep (без токена, образ по хешу) + CodeQL | при High и выше |
| Типы, линтер, формат, тесты | tsc, ESLint + security, Prettier, Vitest | да |
| Зависимости | npm audit (production) + Dependabot | при High/Critical |
| Контейнер | Trivy, Hadolint | при High |
| Инфраструктура | Checkov | да |
| DAST | OWASP ZAP baseline (только PR) | да, через итоговый гейт |
| Заголовки | `scripts/check-security-headers.mjs` | да |
| Скорость | Lighthouse CI (только PR) | да |

---

## 7. GDPR на основном домене

| Что | Как |
|---|---|
| IP посетителей в журнале безопасности | срок 60 дней, основание — законный интерес (ст. 6 ч. 1 п. f), объявлено в политике |
| Cookie | только строго необходимые — согласие не требуется (ePrivacy, ст. 5(3)), баннера нет |
| Сторонние домены | ни одного — IP посетителя третьим лицам не уходит |
| Отзывы | обезличенные, согласие вне сайта (docs/13, раздел 8) |

---

## 8. Если взломали

```
1. ИЗОЛЯЦИЯ (0–15 мин)   Cloudflare → «Under Attack»; снимок диска до любых изменений
2. ОЦЕНКА (15–60 мин)    журнал событий, что затронуто, скомпрометированы ли ключи
3. УСТРАНЕНИЕ (1–6 ч)    ротация всех секретов, откат на чистый образ, закрытие дыры
4. УВЕДОМЛЕНИЕ (≤ 72 ч)  UODO, если есть риск для прав субъектов
5. РАЗБОР (неделя)       тест в CI, который поймал бы это раньше
```

---

## 9. Красные линии

1. **Секрет в git = инцидент.** Ротация ключа обязательна.
2. **Прод-база на личном компьютере — никогда.** Для разработки — только `prisma/seed.ts`.
3. **Ни одного чужого домена в браузере посетителя** — запрещено CSP, а не обещано.
4. **2FA для админского входа** — до выхода в прод.
5. **Бэкап без проверенного восстановления не считается бэкапом.**
6. **`console.log` с персональными данными блокирует слияние.**
7. **Новая зависимость требует обоснования** в описании PR.
8. **В `main` — только через PR с зелёным CI, сливает владелец.** Агент PR не одобряет и не сливает.

---

## 10. Разработка с ИИ-агентом

Код пишет агент (Claude Code, в основном в облаке), одобряет и сливает владелец.
Защита строится так, чтобы ошибка агента или подброшенная ему инструкция
не дошла до `main` и не унесла секреты — даже если агент «согласится».

### 10.1 Рубежи

```
   Агент (облачный контейнер)
       │
   ┌───▼──────────────────────────────────────────────────────────┐
 1 │ СТОРОЖ .claude/hooks/guard-commands.mjs + deny в settings    │  ловит до сети
   │  терминал: push в main, force, --no-verify, токены из env,   │
   │  прямой API GitHub; коннектор: merge, auto-merge, approve,   │
   │  запись файлов через API, запуск workflow по ветке           │
   ├──────────────────────────────────────────────────────────────┤
 2 │ GITHUB PUSH PROTECTION: push с ключом отклоняет сам GitHub   │  до истории
   ├──────────────────────────────────────────────────────────────┤
 3 │ CI: восемь ворот, ни одного секрета в workflow               │  до слияния
   ├──────────────────────────────────────────────────────────────┤
 4 │ RULESET main: только PR, обязательные проверки, без force-   │  граница
   │  push и удаления, без обходов; одобрение — см. 10.2          │
   └──────────────────────────────────────────────────────────────┘
```

Сторож — сеть, а не стена: он в репозитории, и хитрую команду можно
сформулировать мимо разбора. Рубеж 4 на GitHub агенту недоступен: что бы
ни случилось, в `main` попадает только прошедшее CI, история не переписывается.

### 10.2 Аккаунт агента: принятый риск

**Сейчас** агент действует на GitHub от имени владельца. GitHub не отличает
одного от другого, поэтому обязательное одобрение в ruleset включить нельзя:
автор PR не одобряет свой PR.

**Остаточный риск:** если агент обойдёт сторож (ошибка разбора или подброшенная
инструкция), он технически может слить PR. Такой PR всё равно прошёл CI
(рубежи 2–3), а слияние видно в истории и в уведомлениях владельца.

**Решение:** риск принят владельцем 2026-09-25 на время разработки сайта-визитки —
на основном домене нет данных клиентов (docs/13, раздел 10). Пересмотр —
до выхода в прод, вместе с 2FA админки (PROJECT_LOG, A.7 и B.6).

**Целевое состояние** — у агента свой машинный аккаунт:

| | Владелец | Агент (машинный аккаунт) |
|---|---|---|
| Роль в репозитории | owner, единственный в `CODEOWNERS` | collaborator |
| Создаёт ветки и PR | да | да |
| Одобряет PR | да — это и есть подпись | нет: одобрение не от code owner не засчитывается |
| Меняет настройки, ruleset, секреты | да | нет прав |

Ruleset требует одобрения code owner на последний push, и одобрение
сбрасывается новым push: слить можно только тот код, который владелец видел.

### 10.3 Облачное окружение

- Секретов в окружении облачной сессии нет: проекту для `verify` они не нужны.
- Токен GitHub в контейнере есть (его выдаёт облако для git и коннектора);
  сторож запрещает его читать; на случай обхода — рубежи 2–4 и принятый риск (10.2).
- Сеть — ограниченный список (npm, GitHub), не полный доступ.
- Сессию готовит `.claude/hooks/session-start.mjs`: `npm ci --ignore-scripts`,
  клиент Prisma, `core.hooksPath` — чтобы проверка секретов перед коммитом
  работала и в облаке.

### 10.4 Что агент не делает никогда

Сливает, одобряет и автосливает PR; пишет в `main`; переписывает историю и
удаляет ветки на GitHub; меняет настройки, ruleset и секреты; запускает
workflow по своей ветке; читает токены из окружения. Правки CI, git-хуков и
правил агента — только по просьбе владельца и с его «да» при push.

---

## 11. Персональные данные сайта-визитки

Сайт — визитка: ни записи, ни оплаты, ни входа для посетителей. Поэтому
персональных данных здесь мало, и каждое место, куда они попадают, известно
поимённо. Требования ниже — техническое понимание GDPR, а не юридическая
консультация; тексты для посетителей готовятся как заглушки и проверяются
юристом до начала реальной практики (п.11.3).

### 11.1 Правила

1. **Минимизация.** Персональные данные — только под конкретную цель из карты
   (п.11.2). Согласие не запрашивается на обработку, которой нет: баннер
   «разрешите аналитику» на сайте без аналитики — неправда.
2. **Карта обязательна.** Новое место, куда попадают персональные данные
   (таблица, лог, бэкап, внешний сервис, cookie), появляется в PR только
   вместе со строкой в карте: что, зачем, основание, срок, чем удаляется.
3. **Тексты идут за кодом.** PR, меняющий обработку данных, в том же PR меняет
   политику конфиденциальности или явно ставит задачу владельцу.
4. **Обработчики поимённо.** Каждый внешний сервис, через который проходят
   данные посетителей, — в карте, с договором обработки (DPA) и основанием
   передачи за пределы ЕЭЗ, если она есть.
5. **Псевдонимизация — отдельным секретом.** Соль хеша не переиспользует ключи
   подписи и не имеет значения по умолчанию.
6. **Потребитель видит правду.** Польская версия текстов для потребителя
   обязательна, цена — итоговая с валютой, данные владельца (п.11.3) — на
   каждой странице.
7. **Отзывы** — только с записанным согласием автора и с указанием, проверяет
   ли владелец, что отзыв оставил реальный клиент, и как.
8. **Сторонний код — по хешу.** Actions — по SHA коммита, образы — по digest,
   `:latest` запрещён.
9. **Журнал инцидентов и тест баланса интересов** ведутся как документы
   владельца, вне публичного репозитория.
10. **Доступность** — цель WCAG 2.1 AA; у каждой страницы верный `lang`.

### 11.2 Карта

| Где | Что | Зачем и основание | Срок | Чем удаляется | |
|---|---|---|---|---|:---:|
| `security_events`, `honeypot_hits`, `security_incidents` | IP, User-Agent, путь | защита сайта, ст. 6(1)(f) | 60 дней | `recorder.ts`, раз в час | ✅ |
| `ip_blocklist` | IP | защита сайта, ст. 6(1)(f) | 60 дней с последнего события, если блокировка уже снята | `recorder.ts`, раз в час | ✅ |
| `site_settings` | данные владельца: имя, адрес, NIP, контакты | сведения об услугодателе, обязанность по закону (ст. 6(1)(c)) | пока ведётся деятельность | админка | ✅ |
| `admin_login_tokens` | IP владельца при входе | вход в админку, ст. 6(1)(f) | до истечения ссылки | при выдаче новой | ✅ |
| логи nginx | IP, User-Agent, referer, страна | защита и диагностика, ст. 6(1)(f) | 14 дней | `rotate-logs.sh` в контейнере backup, раз в сутки | ✅ |
| логи контейнеров | ошибки приложения | диагностика, ст. 6(1)(f) | до 50 МБ на сервис | Docker, `json-file` 5 × 10 МБ | ✅ |
| бэкапы | копия базы | восстановление, ст. 6(1)(f) | 30 дней | `backup.sh` | ✅ |
| Cloudflare | весь трафик, IP, cookie `__cf_bm`, `cf_clearance` | CDN и защита, ст. 6(1)(f); обработчик, США | по DPA | — | ⛔ DPA |
| хостинг | всё, что выше | обработчик | по DPA | — | ⛔ DPA |
| переписка (почта, мессенджеры) | контакт, содержание | ответ на запрос, ст. 6(1)(b); мессенджер — самостоятельный администратор | до завершения вопроса, дальше — ⟦ЮРИСТ⟧ | вручную | ✅ в политике |
| cookie языка (задача 1) | выбранный язык | настройка по щелчку, ePrivacy 5(3) — без согласия | 1 год | браузер | ⬜ задача 1 |
| отзывы (задача 6) | псевдоним, текст, дата согласия | согласие автора (ст. 6(1)(a), 9(2)(a)) | до отзыва согласия | админка | ⬜ задача 6 |

### 11.3 Заглушки в текстах

Правовые тексты и данные владельца пишутся заранее, до реальной практики, и
к запуску проверяются юристом. Чтобы ничего не потерялось:

- **Данные владельца** (имя, адрес, NIP, профессиональный статус, контакты)
  живут в базе, в профиле владельца, и правятся в админке. В репозитории их
  нет (правило 2 `CLAUDE.md`).
- **В текстах — метки** вида `{{owner.fullName}}`. Сайт подставляет значение
  из профиля; незаполненная метка видна на странице как `⟦не заполнено: …⟧`,
  а не молча пропадает.
- **Режим «в разработке»** — флаг профиля. Пока он включён, на каждой странице
  плашка «сайт в разработке, услуги не оказываются», а у правовых текстов —
  пометка «образец, не юридическая консультация». Выключить флаг нельзя,
  пока остаётся хоть одна незаполненная обязательная метка.
- **Место для юриста** помечено в тексте явно: `⟦ЮРИСТ: …⟧` — что проверить.
