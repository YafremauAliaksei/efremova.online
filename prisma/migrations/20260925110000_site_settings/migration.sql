-- ═══════════════════════════════════════════════════════════════════════════
--  Профиль владельца и режим сайта (docs/03 п.11.3)
--
--  Данные владельца для меток {{owner.*}} в текстах. Пустая таблица —
--  нормальное состояние: сайт в режиме «в разработке», метки видны
--  как «не заполнено».
--
--  Написана руками: движок схем Prisma в облачной сессии недоступен.
--  Типы — как у сгенерированных миграций проекта (TIMESTAMPTZ, TEXT).
-- ═══════════════════════════════════════════════════════════════════════════

CREATE TABLE "site_settings" (
    "key" TEXT NOT NULL,
    "value" TEXT NOT NULL,
    "updatedAt" TIMESTAMPTZ NOT NULL,

    CONSTRAINT "site_settings_pkey" PRIMARY KEY ("key")
);
