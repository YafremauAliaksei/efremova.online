-- ═══════════════════════════════════════════════════════════════════════════
--  Журнал согласий на cookie удаляется целиком
--
--  Сайт-визитка ставит только технически необходимые cookie, а на них
--  согласие не нужно (ePrivacy, ст. 5(3)). Журнал хранил хеш IP, User-Agent
--  и идентификатор посетителя ради квитанции о согласии на обработку,
--  которой нет, — лишние персональные данные (docs/03 п.11.1, правило 1).
--
--  Написана руками: движок схем Prisma в облачной сессии недоступен,
--  а изменение — одна таблица без внешних ключей. Индекс и первичный ключ
--  удаляются вместе с ней.
-- ═══════════════════════════════════════════════════════════════════════════

DROP TABLE "cookie_consents";
